# Legacy PCE stage configurations

这些 S0–S3 配置属于早期 Preference Collapse Exploitability 计划，包含从本地 RTX 4060 到多卡 A100/H100 的旧设想。

它们仅用于复现历史实验，不再是当前研究入口。新实验必须使用 `configs/current/` 与根目录 `PLAN.md`，并遵循先 24GB 低成本 Gate、后 48/80GB 确认的租卡策略。
